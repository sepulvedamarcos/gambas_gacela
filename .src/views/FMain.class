' Gambas class file

Private $configuration As Configuration
Private $log4Gambas As Log4Gambas3
Private $photoService As PhotoService

Public Sub _new(configuration As Configuration, logger As Log4Gambas3, photoService As PhotoService)

  $configuration = configuration
  $log4Gambas = logger
  $photoService = photoService

End

Public Sub Form_Open()

  Dim photos As PhotoEntity[]
  Dim photo As PhotoEntity
  Dim PanelPhoto As Panel
  Dim formphoto As FPhoto

  $log4Gambas.Debug("en formulario open obtenemos los registros de fotos")

  photos = $photoService.GetAllPhotos()
  For Each photo In photos
    PanelPhoto = New Panel(PanelDespliegue)
    PanelPhoto.Width = 266
    PanelPhoto.Height = 336
    PanelPhoto.Border = Border.Solid
    PanelPhoto.Show()

    formphoto = New FPhoto(photo, PanelPhoto)

  Next

End

Public Sub Form_Show()

  $log4Gambas.Debug("en formulario show")

End

Public Sub PanelIzquierdo_MouseDown()

End

Public Sub ButtonBox1_Click()

  Message.Info("hola")

End

Public Sub Expander1_Show()

  Expander1.Expand = True

End

Public Sub Button1_Click()

  Dim foto As String = "/home/marcos/Imágenes/Wallpaper-HD/img-60.jpg"
  Dim photoEntity As PhotoEntity

  photoEntity = $photoService.ImportPhoto(foto)

End
