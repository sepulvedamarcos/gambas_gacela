' Gambas class file

Private $photo As PhotoEntity

Public Sub _new(photo As PhotoEntity)

  Dim cc As Image

  $photo = photo

  LabelPhotoName.Text = $photo.FileName
  LabelPhotoDescription.Text = $photo.Description
  PictureBoxPhoto.Image = Image.FromString($photo.CompressedPhoto)

End
